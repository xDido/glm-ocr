- [User domain familiarity](user_domain_familiarity.md) — learning load-test/observability vocabulary; prefers concepts + charts over raw JSON
- [Tightly scope exploration](feedback_scoping.md) — when user gives file:line anchors in the prompt, verify from there; don't spawn broad Explore agents that re-discover known targets
- [Ship-and-measure for perf optimizations](feedback_ship_and_measure.md) — if user overrides my "skip this" recommendation, implement with env-flag rollback + explicit numeric criterion; don't re-litigate
- [Matrix noise — average over 2+ runs](feedback_matrix_noise.md) — single asyncio-matrix runs are ±15–25% on rps; never commit keep/rollback on one run; rps is tail-dominated, always compare p50+p99 alongside
- [Layout ONNX forward is Conv-dominated](project_layout_profile.md) — 76% of per-call wall time is Conv kernels; next layout optimization should target the kernel (Dnnl/OpenVINO EP, int8, lower input res), not graph surgery
- [OpenVINOExecutionProvider rejected for PP-DocLayoutV3](project_openvino_ep.md) — apparent "3x win" was 15% silent empty-response inflation; root cause is batch=1 baked into the model's torch source. Fix is a detector swap (DocLayout-YOLO), not a provider swap.
- [SGL_SCHEDULE_POLICY=lpm wins over fcfs on this stack](project_sgl_schedule_policy.md) — fcfs -16 to -28% rps at c=12/24, trips capacity cliff at c=32/40. Layout Conv absorbs any SGLang queue-wait saving. Don't re-run without a detector swap or prompt-prefix change.
- [Kernel-level layout optimization 2026-04-23](project_kernel_levers_2026_04_23.md) — LAYOUT_INPUT_SIZE=640x640 reverted (parity validated only on OmniDocBench; unsafe default for passport/ID/receipt distributions). PTQ int8 rejected (DETR head too fragile). ORT fusion verified as no-op. Next structural lever is DocLayout-YOLO (Phase 1).
- [GPU utilization A/B/C/D 2026-04-23](project_gpu_utilization_2026_04_23.md) — SGLang running batch peaks at 11-16 because CPU layout stage is binding (bursty arrivals), not scheduler throttling. Tried spec-off/cuda_graph_max_bs=32/schedule_conservativeness=0.5/context_length=10240 — all net-negative. Lever for raising sustained running count is faster CPU layout stage, not GPU scheduler tuning.
- [DocLayout-YOLO slower than PP-DocLayoutV3 on this CPU](project_doclayout_yolo_probe_2026_04_23.md) — probe invalidated the "3× faster" assumption. On Ryzen 5600X: 3× slower @ imgsz=1024, 11% slower @ 640, only 1.32× faster @ 512 (where quality degrades). Don't swap detectors on this hardware. Re-evaluate if project moves to AVX-512 VNNI CPU or GPU with spare VRAM.
- [CUDA IPC Transport + MTP findings 2026-04-23](project_cuda_ipc_mtp_2026_04_23.md) — MTP already effectively on (NEXTN→EAGLE alias). CUDA IPC crash-loops on 8 GB 3060 Ti (multi-GB MmItemMemoryPool allocation); plumbing kept in docker-compose for ≥16 GB retry on Fargate.
- [FastAPI async sidecar rejected 2026-04-23](project_async_sidecar_2026_04_23.md) — LAYOUT_ASYNC=true net-neutral/worse vs gunicorn baseline; introduced HealthWatchdog fails at c=40 and ServerDisconnectedError at c=64. Running count unchanged. Framework-level swaps (LitServe, RayServe) hit same CPU-kernel ceiling.
